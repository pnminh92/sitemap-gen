require "spec_helper"

describe Sitemap::Gen do
  it "has a version number" do
    expect(Sitemap::Gen::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
